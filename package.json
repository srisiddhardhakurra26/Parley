{
  "name": "parley",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "An AI job platform where a candidate's agent and an employer's agent parley — talk like real people, gather verifiable info, and report back to their humans. Humans decide; agents are bounded gofers.",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "seed": "tsx src/seed.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
